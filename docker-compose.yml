version: "3.4"
networks:
  default-net:
    driver: bridge

services:
  mysql:
    build: config/mysql
    container_name: ${COMPOSE_PROJECT_NAME}_mysql
    environment:
    - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
    - MYSQL_DATABASE=${MYSQL_DATABASE}
    - MYSQL_USER=${MYSQL_USER}
    - MYSQL_PASSWORD=${MYSQL_PASSWORD}
    volumes:
    - ./mysql-db:/var/lib/mysql
    - ./logs/mysql/:/var/log/mysql
    ports:
    - "3306"
    networks:
      - default-net

  webserver:
    build: config/nginx
    container_name: ${COMPOSE_PROJECT_NAME}_webserver
    working_dir: /application
    volumes:
    - ./app:/application
    - ./config/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    - ./logs/nginx:/var/log/nginx
    depends_on:
      - "mysql"
      - "php-fpm"
    ports:
    - "${NGINX_EXTERNAL_PORT}:80"
    networks:
      - default-net

  php-fpm:
    build: config/php-fpm/${PHP_VERSION}
    container_name: ${COMPOSE_PROJECT_NAME}_php-fpm
    working_dir: /application
    volumes:
    - ./app:/application
    - ./config/php-fpm/${PHP_VERSION}/php-fpm.ini:/etc/php/${PHP_VERSION}/fpm/conf.d/99-overrides.ini
    networks:
      - default-net

  redis:
    image: "redis:alpine"
    networks:
      - default-net
    ports:
      - "${REDIS_EXTERNAL_PORT}:6379"

  composer:
    restart: 'no'
    image: composer:latest
    container_name: ${COMPOSE_PROJECT_NAME}_composer
    working_dir: /application
    command: bash -c "composer install --ignore-platform-reqs --no-scripts && composer du"
    volumes:
      - ./app:/application

